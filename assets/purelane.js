(function () {
  "use strict";

  // The live Purelane homepage is managed by purelane-exact.js. Keep this legacy
  // asset inert so accidental double-includes cannot attach duplicate listeners.
})();
